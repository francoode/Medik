{% extends 'front.html.twig' %}

{%  block body %}
    <h1>Itemtipoanalisi</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ itemTipoAnalisi.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ itemTipoAnalisi.nombre }}</td>
            </tr>
            <tr>
                <th>Valorreferencia</th>
                <td>{{ itemTipoAnalisi.valorReferencia }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('itemtipoanalisis_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('itemtipoanalisis_edit', { 'id': itemTipoAnalisi.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
