{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="text-info">Ver Tipo Análisis</h2>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoAnalisi.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoAnalisi.nombre }}</td>
            </tr>
            <tr>
                <th>Metodo</th>
                <td>{{ tipoAnalisi.metodo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoanalisis_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoanalisis_edit', { 'id': tipoAnalisi.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
